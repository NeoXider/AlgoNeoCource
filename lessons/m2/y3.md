# Модуль 2, Урок 3: Арифметические операции

---

## Теория 1: Базовые операции и деление

В C# мы можем выполнять математические операции над переменными, прямо как на уроке математики. Основных бинарных (требующих два операнда) операций пять.

- `+` (сложение): `int result = 10 + 5; // result = 15`
- `-` (вычитание): `int result = 10 - 5; // result = 5`
- `*` (умножение): `int result = 10 * 5; // result = 50`
- `/` (деление): `int result = 10 / 5; // result = 2`
- `%` (остаток от деления): `int result = 10 % 3; // result = 1`

---

### Подробнее про остаток от деления (%)

Представьте, что у вас есть **10 яблок** и **3 друга**. Вы хотите разделить яблоки поровну. Каждому другу вы дадите по 3 яблока, и у вас в руках **останется 1 яблоко**.

Оператор `%` как раз и вычисляет этот остаток.

**Примеры:**
- `10 % 3` вернёт `1` (10 яблок, 3 друга -> 1 в остатке).
- `12 % 5` вернёт `2` (12 яблок, 5 друзей -> 2 в остатке).
- `10 % 2` вернёт `0` (10 яблок, 2 друга -> 0 в остатке).

Если остаток равен 0, значит число разделилось нацело. Это очень полезно, например, чтобы проверить, является ли число чётным!

---

### 💡 Лайфхак: Целочисленное деление

Будьте очень внимательны при делении! Если вы делите одно целое число (`int`) на другое, результат тоже будет целым, а вся дробная часть просто отбросится.

```csharp
// Неправильно! z будет равен 2, а не 2.5
float z = 10 / 4;
```

Чтобы получить точный результат, хотя бы один из операндов должен быть дробным числом. Проще всего добавить суффикс `f` к числу:

```csharp
// Правильно! z будет равен 2.5
float z = 10f / 4f;
```

---

## Практика 1

---

### Часть 1: Проверка знаний

**Вопрос 1:** Чему будет равна переменная `result` после выполнения кода `int result = 14 % 4;`?
A) 3.5
B) 3
C) 2

**Вопрос 2:** Какой результат будет в переменной `z` в коде `float z = 20 / 8;`?
A) 2.5
B) 2
C) 3

**Вопрос 3:** Что выведется в консоль в результате `Debug.Log(5 + 3 * 2);`?
A) 16
B) 11
C) 13

---

### Практика 1: Ответы

**Ответ на Вопрос 1:** C) 2
*   **Пояснение:** 14 разделить на 4 — это 3 и **2 в остатке**.

**Ответ на Вопрос 2:** B) 2
*   **Пояснение:** Ловушка целочисленного деления. Результат операции `20 / 8` будет целым числом (2), так как оба операнда — целые числа.

**Ответ на Вопрос 3:** B) 11
*   **Пояснение:** Операция умножения (`*`) имеет более высокий приоритет, чем сложение (`+`), поэтому сначала выполнится `3 * 2 = 6`, а затем `5 + 6 = 11`.

---

### Часть 2: Применение в игре

Теперь заставим наш кликер работать. Мы будем использовать "магический" метод `OnMouseDown()`, который Unity вызывает при клике на объект с коллайдером.

**Задача:**
1. Откройте скрипт `ClickerGame.cs`.
2. Создайте метод `void OnMouseDown()`.
3. Внутри этого метода увеличьте значение `score` на `clickPower`, используя оператор `+=`.
4. Выведите новый счет в консоль, чтобы убедиться, что все работает.

```check
rules:
  - filename: "ClickerGame.cs"
  - contains: "void OnMouseDown()"
  - contains: "score += clickPower"
  - contains: "Debug.Log"
```

---

## Перерыв

---

## Теория 2: Сокращенные операции и приоритеты

Чтобы делать код короче и чище, в C# есть специальные операторы.

---
### Инкремент и декремент (`++`, `--`)
Это операции для увеличения или уменьшения числового значения на 1.

**Постфиксный `x++` (сначала вернуть, потом увеличить):**
```csharp
int x = 5;
// Сначала z получит значение x (5), и только потом x увеличится до 6.
int z = x++;
Debug.Log($"x = {x}, z = {z}"); // Выведет: x = 6, z = 5
```

**Префиксный `++x` (сначала увеличить, потом вернуть):**
```csharp
int x = 5;
// Сначала x увеличится до 6, и это новое значение получит z.
int z = ++x;
Debug.Log($"x = {x}, z = {z}"); // Выведет: x = 6, z = 6
```
Аналогично работают `x--` и `--x`.

---
### Составное присваивание (`+=`, `-=`)
Это удобные сокращения для частых операций.

Вместо `score = score + 10;` можно написать короче:
```csharp
score += 10; // Прибавить 10 к score
```
Это работает для всех основных операций:
- `x -= y` (вычитание)
- `x *= y` (умножение)
- `x /= y` (деление)

---

## Практика 2

---

### Часть 1: Проверка знаний

**Вопрос 1:** Какое значение будет в переменной `z` после выполнения кода `int x = 5; int z = x++;`?
A) 6
B) 5
C) 4

**Вопрос 2:** Что будет в переменной `d` после `int d = 10 - (3 + 2) * 2;`?
A) 0
B) 4
C) 14

---

### Практика 2: Ответы

**Ответ на Вопрос 1:** B) 5
*   **Пояснение:** Это постфиксный инкремент. Сначала значение `x` (равное 5) присваивается переменной `z`, и только **потом** `x` увеличивается до 6.

**Ответ на Вопрос 2:** A) 0
*   **Пояснение:** Сначала выполняются скобки (3+2=5), затем умножение (5*2=10), и в конце вычитание (10-10=0).

---

### Часть 2: Применение в игре

Давайте используем сокращенные операции, чтобы добавить новое поведение в наш кликер.

**Задача:**
1. В скрипте `ClickerGame` добавьте публичную переменную `penaltyAmount` типа `int` со значением `15`.
2. В методе `Update()` (который выполняется каждый кадр) уменьшайте `score` на 1 каждую секунду. Для этого используйте `Time.deltaTime`.

**Код должен выглядеть так:**
```csharp
void Update()
{
    score -= 1 * Time.deltaTime;
}
```

```check
rules:
  - filename: "ClickerGame.cs"
  - contains: "void Update()"
  - contains: "score -= "
  - contains: "Time.deltaTime"
```

---

## Теория 3: Вывод информации на экран (UI)

Выводить данные в консоль полезно для отладки, но игрок этого не видит. Чтобы показать счет на экране, нам нужно связать наш скрипт с UI-элементом.

Мы будем использовать **TextMeshPro** — мощный инструмент для работы с текстом в Unity.

**Шаги для связи кода и текста:**

1.  **Подключить библиотеку:** В самом верху скрипта, рядом с `using UnityEngine;`, нужно добавить строку `using TMPro;`.
2.  **Создать публичную переменную:** В классе нужно объявить публичную переменную типа `TMP_Text`. Это создаст в Инспекторе специальное поле, куда можно будет перетащить наш текстовый объект со сцены.
    ```csharp
    public TMP_Text scoreText;
    ```
3.  **Обновить текст в коде:** У текстового объекта есть свойство `.text`, которое хранит отображаемый им текст. Это свойство имеет тип `string`. Мы можем присвоить ему новое значение, чтобы обновить то, что видит игрок.
    ```csharp
    scoreText.text = "Счет: " + score;
    ```

---

### 💡 Лайфхак: Сложение текста и чисел

Вы заметили, что мы пишем `"Счет: " + score`? Здесь `"Счет: "` — это текст (`string`), а `score` — это число (`int`).

Когда C# видит операцию `+` между строкой и чем-то еще (числом, булевым значением), он проявляет смекалку: он автоматически превращает это число в строку и "склеивает" (конкатенирует) две строки в одну.

`"Счет: " + 10 + 5` → `("Счет: " + "10") + "5"` → `"Счет: 105"`

Если же вам нужно сначала выполнить математическое действие, а потом превратить результат в текст, используйте скобки `()`:

`"Счет: " + (10 + 5)` → `"Счет: " + 15` → `"Счет: 15"`

Это очень удобный способ для форматирования и вывода данных!

---

## Практика 3

---

### Часть 1: Проверка знаний

**Вопрос 1:** Какую строку нужно добавить в начало файла для работы с TextMeshPro?
A) `using UnityEngine.UI;`
B) `using TMPro;`
C) `using TextMeshPro;`

**Вопрос 2:** Какое свойство у переменной типа `TMP_Text` нужно изменить, чтобы обновить текст на экране?
A) `.value`
B) `.string`
C) `.text`

---

### Практика 3: Ответы

**Ответ на Вопрос 1:** B) `using TMPro;`
*   **Пояснение:** Это пространство имен, в котором содержатся все классы для работы с TextMeshPro.

**Ответ на Вопрос 2:** C) `.text`
*   **Пояснение:** Именно свойство `.text` хранит текстовое содержимое UI-элемента.

---

### Часть 2: Применение в игре

Давайте выведем счет на экран.

**Задача:**
1. На сцене создайте текстовый объект **UI -> Text - TextMeshPro**.
2. В скрипте `ClickerGame.cs` подключите библиотеку `TMPro`.
3. Создайте публичную переменную `scoreText` типа `TMP_Text`.
4. В Unity, перетащите ваш текстовый объект со сцены в поле `Score Text` у скрипта в Инспекторе.
5. В методе `Update()`, после уменьшения счета, добавьте строку, которая будет обновлять текст на экране, показывая текущий `score`.

```check
rules:
  - filename: "ClickerGame.cs"
  - contains: "using TMPro;"
  - contains: "public TMP_Text scoreText;"
  - contains: "scoreText.text"
```

---

## Итоги

На этом уроке мы изучили:
- Базовые арифметические операции и их особенности.
- Как заставить игру реагировать на клики мыши с помощью `OnMouseDown()`.
- Сокращенные операции `++`, `--`, `+=` для чистоты кода.
- Как связывать код с UI-элементами TextMeshPro для отображения информации игроку.
- Как C# автоматически преобразует типы при сложении строк и чисел.
