# Модуль 4, Урок 4: Крестики-нолики — Random.Range, бот и временный режим игры

**Цель урока:** познакомиться с `Random.Range`, научиться собирать список свободных клеток и сделать простого бота, который умеет выбирать допустимый ход во временной версии игры против компьютера.

После прошлого урока у нас уже есть полноценная игра на двоих.
Теперь добавим новый игровой опыт: партия против компьютера, который тоже умеет делать ходы по правилам.

---

## Как сделать, чтобы в крестики-нолики можно было играть не только вдвоём?

Игра уже умеет:

- ставить `X` и `O`;
- проверять победу;
- определять ничью.

Теперь хочется, чтобы у игрока появился противник.

---

Компьютерному противнику тоже нужно принимать решения:

- куда ходить;
- как не попасть в занятую клетку;
- как выбрать один вариант из нескольких.

---

## Как игре выбрать одну клетку из нескольких свободных?

Если на поле осталось несколько свободных клеток, боту нужно выбрать одну из них.
Сделать это можно через случайность.

Но сначала игра должна понять:

- какие клетки вообще свободны;
- из чего можно выбирать.

---

## Теория 1: Как случайность работает в играх

Бот тоже должен принимать решения, но для первого варианта игры ему не нужна сложная стратегия.
Ему достаточно уметь выбирать один допустимый ход из нескольких свободных.

В играх случайность используют очень часто:

- для выбора награды;
- для случайного поведения врага;
- для появления объектов;
- для выбора одного варианта из нескольких.

В Unity для этого часто используют:

```csharp
Random.Range(0, freeCells.Count)
```

---

Но у случайности есть важное правило:

сначала нужно собрать **только допустимые варианты**,
и только потом выбирать из них случайный.

Если бот будет брать число сразу из `0..8`, он может попробовать пойти в уже занятую клетку.

---

```quiz
id: l2m4y4-q-random-range
kind: single
text: Что делает Random.Range в этом уроке?
answers:
  - text: Помогает выбрать случайный индекс из списка свободных клеток
    correct: true
  - text: Проверяет победу по диагонали
  - text: Создаёт кнопку на Canvas
```

---

## Пояснение (Random.Range)

- Случайность полезна только тогда, когда мы выбираем из корректного набора вариантов.

---

```quiz
id: l2m4y4-q-free-cells-first
kind: truefalse
text: Бот может сразу выбирать случайное число от 0 до 8 без проверки свободных клеток.
answers:
  - text: True
  - text: False
    correct: true
```

---

## Пояснение (сначала свободные клетки)

- Если сначала не отфильтровать свободные клетки, бот будет делать неверные ходы.

---

```quiz
id: l2m4y4-q-random-usage
kind: multiple
text: Где в играх вообще полезна случайность? (выберите 2)
answers:
  - text: Выбор награды
    correct: true
  - text: Простое поведение противника
    correct: true
  - text: Замена всех if в игре
  - text: Автоматическое создание Canvas
```

---

## Пояснение (где используется случайность)

- Случайность хорошо работает там, где у игры есть несколько допустимых вариантов поведения.

---

## Практика 1: Пишем отдельный скрипт бота

Сейчас сделаем так, чтобы выбор хода жил в отдельном скрипте.
После этой практики у нас появится бот, который умеет сам находить свободные клетки и выбирать одну из них.

---

### Шаг 1: Создайте `TicTacToeBot.cs`

Сделайте новый скрипт:

```csharp
using System.Collections.Generic;
using UnityEngine;

public class TicTacToeBot : MonoBehaviour
{
}
```

```check
rules:
  - filename: "TicTacToeBot.cs"
  - contains: "using System.Collections.Generic;"
  - contains: "public class TicTacToeBot"
```

---

### Шаг 2: `GetFreeCells(int[] board)`

Сделайте метод, который проходит по массиву поля и собирает индексы всех пустых клеток в список.

Это хороший пример метода, который возвращает **не bool**, а целый список:

```csharp
public List<int> GetFreeCells(int[] board)
```

```check
rules:
  - filename: "TicTacToeBot.cs"
  - contains: "GetFreeCells"
  - contains: "List<int>"
  - contains: "freeCells.Add(i)"
```

---

### Шаг 3: `GetRandomMove(List<int> freeCells)`

Теперь сделайте метод, который получает список свободных клеток и возвращает **один случайный индекс**:

```csharp
public int GetRandomMove(List<int> freeCells)
{
    int randomIndex = Random.Range(0, freeCells.Count);
    return freeCells[randomIndex];
}
```

```check
rules:
  - filename: "TicTacToeBot.cs"
  - contains: "GetRandomMove"
  - contains: "Random.Range"
  - contains: "return freeCells[randomIndex]"
```

---

### Шаг 4: `ChooseMove(int[] board)`

Сделайте главный метод бота:

1. получить список свободных клеток;
2. если список пустой, вернуть `-1`;
3. иначе вернуть случайный допустимый ход.

```check
rules:
  - filename: "TicTacToeBot.cs"
  - contains: "ChooseMove"
  - contains: "if (freeCells.Count == 0)"
  - contains: "return -1"
```

---

### 💡 Лайфхак: простой бот — это нормально

В учебной игре бот не обязан быть умным.
Если он делает **допустимый** ход и дети понимают, как он устроен, значит урок уже работает хорошо.

---

## Перерыв

---

Теперь бот умеет выбирать ход.
Осталось встроить его в игру.

---

## Как встроить бота так, чтобы он ходил в нужный момент?

У игры уже есть свой порядок действий:

1. игрок делает ход;
2. поле обновляется;
3. игра проверяет правила.

Теперь в этот порядок нужно аккуратно добавить и ход бота.

---

## Теория 2: Методы могут возвращать не только bool

В прошлых уроках методы уже возвращали ответ `да/нет`.
Теперь на игровом примере хорошо видно, что метод может вернуть и список вариантов, и один конкретный ход.

На прошлом уроке были методы, которые возвращали `bool`:

- `CheckWinner(...)`
- `MatchesLine(...)`
- `IsBoardFull()`

В этом уроке появятся и другие варианты:

- метод может вернуть `List<int>`;
- метод может вернуть `int`.

---

Это удобно читать так:

- `GetFreeCells(...)` возвращает список свободных клеток;
- `GetRandomMove(...)` возвращает один выбранный индекс;
- `ChooseMove(...)` возвращает итоговый ход бота.

То есть методы могут возвращать не только ответ `да/нет`, но и полезные данные.

---

```quiz
id: l2m4y4-q-return-list
kind: single
text: Что возвращает метод GetFreeCells(int[] board)?
answers:
  - text: Список индексов свободных клеток
    correct: true
  - text: Текст победы
  - text: Цвет кнопки
```

---

## Пояснение (метод возвращает список)

- Метод может вернуть не только `bool`, но и список, число, строку и другие данные.

---

```quiz
id: l2m4y4-q-return-int
kind: truefalse
text: Метод GetRandomMove(...) удобно сделать с типом int, потому что он возвращает один индекс клетки.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (метод возвращает число)

- Когда нужно вернуть один номер клетки, тип `int` подходит лучше всего.

---

## Когда бот должен делать ход?

Бот не должен ходить в любой момент.
Игра должна вызывать его только тогда, когда:

- ход игрока уже закончился;
- партия ещё не завершена;
- сейчас очередь бота.

---

## Теория 3: Как подключить бота к уже готовой логике

Когда бот уже умеет выбирать ход, остаётся самый важный шаг: встроить его в игру так, чтобы он не ломал готовые правила партии.
То есть бот должен использовать уже существующую логику, а не жить отдельно от неё.

Удобный порядок такой:

1. игрок делает ход;
2. `MakeMove(...)` обновляет поле;
3. игра проверяет победу и ничью;
4. если партия продолжается, а сейчас очередь бота, вызывается `MakeBotMove()`.

---

Внутри `MakeBotMove()` логика простая:

1. бот просит у себя ход через `ChooseMove(board)`;
2. если индекс корректный, игра делает ход через обычный `MakeMove(...)`.

Так бот не пишет правила игры заново, а использует уже существующую логику партии.

---

```quiz
id: l2m4y4-q-when-bot-moves
kind: single
text: Когда бот должен делать ход?
answers:
  - text: После хода игрока, если игра ещё не окончена
    correct: true
  - text: До старта сцены
  - text: Только после перезагрузки Unity
```

---

## Пояснение (момент хода бота)

- Бот не должен ходить в момент победы или ничьей.
- Сначала всегда нужно проверить состояние игры.

---

```quiz
id: l2m4y4-q-why-minus-one
kind: multiple
text: Зачем бот может возвращать -1? (выберите 2)
answers:
  - text: Чтобы показать, что подходящего хода нет
    correct: true
  - text: Чтобы игра могла не делать лишний ход
    correct: true
  - text: Чтобы автоматически победить
  - text: Чтобы заменить массив board
```

---

## Пояснение (-1)

- `-1` удобно использовать как специальное значение: подходящего хода нет.
- Тогда игра может спокойно проверить это через `if (botMove >= 0)`.

---

## Практика 2: Подключаем бота к игре

Теперь встроим `TicTacToeBot` в уже готовую игру.
После этой практики можно будет запустить временную версию партии против бота прямо через настройки в инспекторе.

---

### Шаг 1: Добавьте ссылку на бота в `TicTacToeGame`

В `TicTacToeGame.cs` добавьте:

```csharp
public TicTacToeBot bot;
public bool playWithBot;
```

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "public TicTacToeBot bot"
  - contains: "playWithBot"
```

---

### Шаг 2: Сделайте `MakeBotMove()`

Метод должен:

1. запросить ход у бота;
2. проверить, что индекс корректный;
3. вызвать `MakeMove(botMove)`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "MakeBotMove"
  - contains: "bot.ChooseMove(board)"
  - contains: "MakeMove(botMove)"
```

---

### Шаг 3: Вызовите бота после хода игрока

После `MakeMove(cellIndex)` в `HandleCellClicked(...)` добавьте проверку:

- включён ли режим бота;
- не закончилась ли игра;
- сейчас ли очередь бота.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "if (playWithBot && !isGameOver && currentPlayer == 2)"
  - contains: "MakeBotMove();"
```

---

### Шаг 4: Подключите компонент в сцене

1. Добавьте `TicTacToeBot` на `GameManager`.
2. В поле `Bot` у `TicTacToeGame` назначьте этот компонент.
3. Включите `Play With Bot` в инспекторе.
4. Проверьте, что после хода игрока бот делает ответный ход.

```check
rules:
  - component_exists:
      object: "GameManager"
      type: "TicTacToeBot"
```

---

### Шаг 5: Проверьте поведение бота

Проверьте, что бот:

- ходит только после игрока;
- выбирает свободную клетку;
- не делает ход после победы;
- не делает ход после ничьей.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "ChooseMove"
  - contains: "MakeBotMove"
```

---

### 💡 Лайфхак: для бота полезно оставлять мало свободных клеток

Очень полезно вручную проверить бота в ситуации, когда свободна только одна клетка.
Так сразу видно, не пытается ли он ходить в занятое место.

---

## Итоги

На уроке мы:

- познакомились с `Random.Range`;
- научились собирать список свободных клеток;
- увидели, что методы могут возвращать `List<int>` и `int`;
- написали отдельный скрипт бота;
- временно встроили режим игры против бота;
- сделали рабочую версию партии против компьютера.
