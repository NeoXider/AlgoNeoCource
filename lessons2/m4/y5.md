# Модуль 4, Урок 5: Крестики-нолики — статус, режимы, визуал и сборка

**Цель урока:** в лёгком финальном уроке добавить выбор между 2 режимами игры, понятный статус, немного дописать UI-связку, а основное время оставить на визуальную доработку, тестирование и сборку.

---

Сейчас у нас уже есть рабочая игра на двоих и временная версия игры с ботом.
На этом уроке нового кода будет немного: мы быстро доведём UI до финального состояния и оставим больше времени на аккуратный внешний вид, проверку игры и билд.

---

## Как сделать, чтобы игрок сразу понимал, что происходит на экране?

Даже хорошая логика ощущается слабее, если игрок не видит:

- чей сейчас ход;
- какой режим выбран;
- закончилась партия или нет.

Значит, игре нужен понятный интерфейс.

---

## Как одной кнопкой запустить нужный режим?

У нас уже есть вся основная игровая логика.
Теперь хочется, чтобы игроку не приходилось включать режим бота вручную в инспекторе.

Удобнее дать ему простой выбор прямо на экране.

---

## Теория 1: UI связывает игрока и игровую логику

До этого вся сила игры была в логике.
Теперь нужно сделать так, чтобы игроку было удобно этой логикой пользоваться с первого экрана.

На этом уроке мы добавляем три важные части интерфейса:

- `StatusText` — показывает, что происходит в партии;
- `ModePanel` — экран выбора режима;
- `GamePanel` — само игровое поле.

Для управления ими удобно сделать `TicTacToeUI.cs`.

---

В этом скрипте полезно хранить ссылки на:

- текст статуса;
- панель выбора режима;
- панель игры;
- кнопку игры вдвоём;
- кнопку игры против бота.

Так UI будет отдельно управлять экраном, а сама игра останется в `TicTacToeGame`.

---

```quiz
id: l2m4y5-q-status-text
kind: single
text: Зачем в игре нужен текст статуса?
answers:
  - text: Чтобы игрок понимал, что происходит в партии прямо сейчас
    correct: true
  - text: Только для украшения инспектора
  - text: Чтобы заменять массив board
```

---

## Пояснение (статус)

- Хороший UI не просто красивый, а понятный.
- Статус помогает читать состояние партии без догадок.

---

```quiz
id: l2m4y5-q-two-modes
kind: multiple
text: Какие режимы мы добавляем на этом уроке? (выберите 2)
answers:
  - text: Игра вдвоём
    correct: true
  - text: Игра против бота
    correct: true
  - text: Сетевая игра
  - text: Режим открытого мира
```

---

## Пояснение (режимы)

- Для этого проекта достаточно двух понятных режимов.
- За счёт них игра уже ощущается завершённой.

---

## Практика 1: Сразу делаем выбор режима на старте

Сейчас добавим на сцену минимальный, но полезный UI, чтобы уже в начале игры можно было выбрать режим.
После этой практики проект станет заметно более "настоящей игрой", а не просто сценой для теста логики.

---

### Шаг 1: Создайте элементы на сцене

Создайте:

- `StatusText`
- `ModePanel`
- `GamePanel`

Внутри `ModePanel` добавьте две кнопки:

- `TwoPlayersButton`
- `BotButton`

```check
rules:
  - object_exists: "StatusText"
  - object_exists: "ModePanel"
  - object_exists: "GamePanel"
```

---

### Шаг 2: Подготовьте `TicTacToeUI.cs`

Если скрипта ещё нет, создайте его.
Добавьте в него ссылки на UI-элементы:

- `statusText`
- `modePanel`
- `gamePanel`
- `twoPlayersButton`
- `botButton`

```check
rules:
  - filename: "TicTacToeUI.cs"
  - contains: "statusText"
  - contains: "modePanel"
  - contains: "gamePanel"
  - contains: "twoPlayersButton"
  - contains: "botButton"
```

---

### Шаг 3: Сделайте три маленьких метода UI

В `TicTacToeUI.cs` добавьте:

- `ShowModeSelection()`
- `ShowGamePanel()`
- `SetStatusText(string text)`

```check
rules:
  - filename: "TicTacToeUI.cs"
  - contains: "ShowModeSelection"
  - contains: "ShowGamePanel"
  - contains: "SetStatusText"
```

---

### Шаг 4: Подключите ссылки в инспекторе

Проверьте, что `TicTacToeUI` получил все нужные ссылки:

- текст;
- панели;
- кнопки;
- ссылку на `TicTacToeGame`.

```check
rules:
  - filename: "TicTacToeUI.cs"
  - contains: "public TicTacToeGame game"
```

---

### 💡 Лайфхак: сначала добейтесь понятности, потом красоты

Если кнопки режима и статус уже работают, проект ощущается завершённым даже без сложного дизайна.
Такой порядок очень помогает не застрять на украшениях раньше времени.

---

## Перерыв

---

Теперь экран уже умеет что-то показывать.
Осталось быстро связать его с готовой логикой, а потом оставить время на полировку проекта.

---

## Как не писать новую игру заново ради выбора режима?

Самое приятное здесь в том, что главная логика уже готова.
Нам не нужно заново делать поле, победу, ничью и бота.

Нужно только аккуратно связать уже готовые части.

---

## Теория 2: Минимум нового кода, максимум повторного использования

Это очень хороший момент, чтобы показать детям важную идею разработки: не всё в новом уроке нужно писать с нуля.
Иногда лучший следующий шаг — правильно собрать уже готовые части в единый удобный результат.

В `TicTacToeGame.cs` уже есть логика партии.
Поэтому здесь не нужно делать большой новый блок механик.
Новые методы должны быть короткими:

- `StartTwoPlayersGame()`
- `StartBotGame()`
- `StartNewMatch()`
- `UpdateStatusText()`
- `ShowModeSelection()`

Их задача не создавать новую механику, а правильно настроить уже существующую.

---

Обычно в этих методах мы:

- выбираем значение `playWithBot`;
- сбрасываем поле;
- показываем нужную панель;
- обновляем текст статуса.

Это хороший пример финальной сборки проекта из уже готовых деталей.
Именно поэтому урок должен ощущаться легче предыдущих: мы не строим новую механику, а аккуратно заканчиваем уже готовую игру.

---

```quiz
id: l2m4y5-q-reuse-logic
kind: truefalse
text: На этом уроке удобнее переиспользовать уже готовую игровую логику, а не писать новую механику заново.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (переиспользование)

- Повторное использование кода экономит время.
- Это один из главных признаков аккуратной архитектуры.

---

```quiz
id: l2m4y5-q-update-status
kind: single
text: Что должна делать UpdateStatusText()?
answers:
  - text: Обновлять текст так, чтобы игрок видел текущее состояние партии
    correct: true
  - text: Создавать новые клетки на сцене
  - text: Собирать билд игры
```

---

## Пояснение (обновление статуса)

- Хороший статус нужен и во время партии, и после её завершения.
- Он делает поведение игры более понятным с первого взгляда.

---

## Практика 2: Быстро связываем режимы и статус

Теперь свяжем кнопки UI с уже готовой игрой.
После этой короткой части у нас уже будет завершённый запуск двух режимов, и можно будет спокойно перейти к полировке проекта.

---

### Шаг 1: Добавьте ссылку на UI в `TicTacToeGame`

В `TicTacToeGame.cs` добавьте поле:

```csharp
public TicTacToeUI ui;
```

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "public TicTacToeUI ui"
```

---

### Шаг 2: Сделайте запуск двух режимов

Добавьте:

- `StartTwoPlayersGame()`
- `StartBotGame()`
- `StartNewMatch()`

Пусть они:

- настраивают `playWithBot`;
- сбрасывают поле;
- показывают игровую панель.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "StartTwoPlayersGame"
  - contains: "StartBotGame"
  - contains: "StartNewMatch"
```

---

### Шаг 3: Сделайте `UpdateStatusText()`

Пусть этот метод обновляет текст для ситуаций:

- ход X;
- ход O;
- ход бота;
- победа;
- ничья.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "UpdateStatusText"
```

---

### Шаг 4: Подключите кнопки режима и проверьте старт игры

В `TicTacToeUI.cs` добавьте методы:

- `OnTwoPlayersClicked()`
- `OnBotClicked()`

Пусть они вызывают методы игры.

```check
rules:
  - filename: "TicTacToeUI.cs"
  - contains: "OnTwoPlayersClicked"
  - contains: "OnBotClicked"
  - filename: "TicTacToeGame.cs"
  - contains: "ShowModeSelection"
  - contains: "UpdateStatusText"
  - contains: "ShowGamePanel"
```

После этого сразу проверьте:

- сначала виден выбор режима;
- после нажатия начинается нужный режим;
- статус обновляется;
- после завершения партии поле больше не ломается.

---

### 💡 Лайфхак: UI лучше проверять с точки зрения игрока

Переделывая экран, полезно спрашивать себя:

- что я вижу первым;
- понятно ли, куда нажимать;
- ясно ли, что случилось после хода.

Так интерфейс становится заметно лучше.

---

## Что делает проект по-настоящему готовым?

Когда логика и UI уже связаны, игру отделяют от финала ещё несколько важных вещей:

- аккуратный внешний вид;
- проверка сценариев;
- реальная сборка.

---

## Теория 3: Полировка, тестирование и билд

Когда механика уже работает, урок всё ещё не закончен.
Финальная ценность проекта часто появляется именно здесь: в удобстве, внешнем виде и в том, что игру можно реально собрать и показать другому человеку.

Финальный этап проекта — это не новая большая механика, а доведение до качества.

Сюда входят:

- цвета и шрифты;
- понятные подписи;
- удобные кнопки;
- проверка игровых сценариев;
- сборка игры.

---

Билд особенно приятно делать в конце, потому что именно он превращает проект из "урока в Unity" в настоящую отдельную игру.

---

```quiz
id: l2m4y5-q-polish
kind: single
text: Что такое полировка игры?
answers:
  - text: Улучшение внешнего вида, удобства и мелких деталей без добавления большой новой механики
    correct: true
  - text: Полная замена Unity на другой движок
  - text: Удаление всех кнопок из сцены
```

---

## Пояснение (полировка)

- Полировка делает игру приятнее и понятнее.
- Это полноценная часть разработки, а не мелочь "на остаток времени".

---

```quiz
id: l2m4y5-q-before-build
kind: multiple
text: Что полезно проверить перед билдом? (выберите 2)
answers:
  - text: Основные игровые сценарии
    correct: true
  - text: Ссылки в инспекторе
    correct: true
  - text: Удаление всех кнопок
  - text: Замена Unity на другой движок
```

---

## Пояснение (перед билдом)

- Билд не чинит ошибки автоматически.
- Если в сцене что-то не подключено, это попадёт и в готовую игру.

---

## Практика 3: Финальная проверка и сборка

Теперь начинается самая спокойная и творческая часть урока.
Именно здесь можно использовать запасное время: довести внешний вид, спокойно проверить игру и собрать билд.

---

### Шаг 1: Пройдите чеклист сценариев

Проверьте:

- игра вдвоём;
- игра против бота;
- победа по строке;
- победа по столбцу;
- победа по диагонали;
- ничья;
- блокировка поля после конца игры.

---

### Шаг 2: Доработайте визуал

Измените хотя бы несколько вещей под свой стиль:

- фон;
- цвет кнопок;
- шрифт;
- оформление статуса;
- экран выбора режима.

---

### Шаг 3: Сделайте билд

1. Добавьте сцену в `Build Settings`.
2. Проверьте платформу.
3. Нажмите `Build`.
4. Запустите готовую игру и проверьте результат.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "StartBotGame"
  - contains: "StartTwoPlayersGame"
  - contains: "UpdateStatusText"
  - filename: "TicTacToeUI.cs"
  - contains: "OnTwoPlayersClicked"
  - contains: "OnBotClicked"
```

---

### 💡 Лайфхак: сыграйте в билд как обычный игрок

Очень полезно пройти 2-3 короткие партии уже в собранной версии игры.
Так сразу видно, всё ли понятно без инспектора, подсказок и редактора Unity.

---

## Итоги

На уроке мы:

- добавили статусный текст;
- сделали выбор между 2 режимами игры;
- аккуратно связали UI и игровую логику;
- доработали внешний вид;
- протестировали основные сценарии;
- собрали финальную версию проекта.
