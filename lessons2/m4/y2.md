# Модуль 4, Урок 2: Крестики-нолики — очередь хода и логика клетки

**Цель урока:** сделать настоящую очередность `X` и `O`, запретить повторный клик по занятой клетке и научить игру аккуратно обрабатывать ход игрока.

На прошлом уроке игра уже умела показывать `X` после клика.
Теперь мы превращаем этот первый клик в настоящее правило игры: у хода появляется порядок, а у клетки появляется состояние.

---

## Как игре понять, чей сейчас ход?

На прошлом уроке по клетке можно было нажать, и в ней появлялся символ.
Теперь игра должна стать умнее:

- понимать, чей сейчас ход;
- ставить разные символы;
- не разрешать нажимать в занятую клетку.

---

Когда появляются правила игры, одного клика уже недостаточно.
Теперь нужно, чтобы код принимал решения по состоянию поля.

---

## Как отличить пустую клетку от занятой?

Если игрок нажал на клетку, игра должна быстро ответить:

- здесь ещё пусто;
- здесь уже стоит `X`;
- здесь уже стоит `O`.

---

## Теория 1: Состояние клетки и текущий игрок

Чтобы крестики-нолики были похожи на настоящую игру, недостаточно просто вывести символ.
Игра должна помнить, кто ходит сейчас и что уже лежит в каждой клетке.

Массив `board` по-прежнему хранит состояние поля.
Теперь читаем его так:

- `0` — пусто
- `1` — X
- `2` — O

Также игре нужна переменная, которая хранит, чей сейчас ход:

```csharp
public int currentPlayer = 1;
```

И ещё одна полезная переменная:

```csharp
public bool isGameOver;
```

Тип `bool` нужен, когда переменная хранит только два варианта ответа:

- да или нет;
- true или false;
- включено или выключено.

Она хранит очень простой ответ:

- `false` — партия ещё идёт;
- `true` — партия уже закончилась.

---

Текст в кнопке нужен игроку, а массив нужен логике.
Если в клетке написано `X`, человеку это видно сразу.
Но для проверки правил и принятия решений игре удобнее работать с числами в массиве.

---

```quiz
id: l2m4y2-q-empty-cell
kind: single
text: Что означает значение 0 в массиве board?
answers:
  - text: Клетка пустая
    correct: true
  - text: В клетке стоит X
  - text: Игра окончена
```

---

## Пояснение (пустая клетка)

- `0` удобно использовать как состояние "ничего нет".
- Тогда игре легко проверить, можно ли делать ход.

---

```quiz
id: l2m4y2-q-current-player
kind: truefalse
text: Переменная currentPlayer помогает игре понимать, чей сейчас ход.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (currentPlayer)

- `currentPlayer` помогает переключаться между X и O.
- Без него оба игрока ставили бы один и тот же символ.

---

```quiz
id: l2m4y2-q-why-board-not-text
kind: multiple
text: Почему для логики игры удобнее использовать массив board, а не просто текст на кнопке? (выберите 2)
answers:
  - text: В массиве удобнее хранить состояние клетки
    correct: true
  - text: Массив проще использовать в проверках
    correct: true
  - text: Потому что TMP_Text нельзя менять
  - text: Потому что массив автоматически рисует UI
```

---

## Пояснение (массив и текст)

- Текст нужен игроку, а массив нужен коду.
- Когда игра читает данные из массива, логика получается надёжнее.

---

## Практика 1: Делаем очередь хода

Сейчас добавим главное правило партии: X и O будут ходить по очереди.
После этой практики игра перестанет ставить только `X` и начнёт отличать двух игроков.

---

### Шаг 1: Новые поля в `TicTacToeGame.cs`

Добавьте:

- `public string oSymbol = "O";`
- `public int currentPlayer = 1;`
- `public bool isGameOver;`

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "oSymbol ="
  - contains: "currentPlayer = 1"
  - contains: "isGameOver"
```

---

### Шаг 2: Обновите `UpdateCell(int cellIndex)`

Теперь клетка должна показывать:

- `X`, если в массиве стоит `1`;
- `O`, если в массиве стоит `2`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "if (board[cellIndex] == 1)"
  - contains: "else if (board[cellIndex] == 2)"
  - contains: "boardCell.SetSymbol(oSymbol)"
```

---

### Шаг 3: Сделайте `MakeMove(int cellIndex)`

Этот метод должен:

1. записать `currentPlayer` в массив;
2. обновить клетку;
3. переключить игрока.

После создания метода найдите метод `HandleCellClicked(int cellIndex)`, удалите старую строчку назначения `board[cellIndex] = 1;` и вместо неё вызовите новый метод: `MakeMove(cellIndex);`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "MakeMove"
  - contains: "board[cellIndex] = currentPlayer"
  - contains: "SwitchPlayer()"
  - contains: "MakeMove(cellIndex)"
```

---

### 💡 Лайфхак: читайте числа как состояния

В таких играх удобно мысленно читать массив так:

- `0` — пусто
- `1` — крестик
- `2` — нолик

Когда дети привыкают читать числа как состояния, игровая логика становится намного понятнее.

---

## Перерыв

---

Теперь игра умеет чередовать X и O.
Осталось сделать так, чтобы неправильные нажатия просто не срабатывали.

---

## Как остановить неправильный ход?

Игрок не должен:

- нажимать на уже занятую клетку;
- ломать ход случайным повторным кликом.

Значит, до выполнения хода игра должна сделать несколько маленьких проверок.

---

## Теория 2: Проверки перед ходом и маленькие методы-действия

Когда правила уже появились, игре нужно научиться не только делать ход, но и вовремя его останавливать.
Именно здесь становятся полезны короткие методы с одной задачей и ранний выход.

Очень удобно, когда у каждого метода есть одна понятная задача:

- `HandleCellClicked(...)` — получает клик;
- `MakeMove(...)` — делает ход;
- `SwitchPlayer()` — переключает игрока;
- `SetInteractable(...)` — включает или выключает клетку.

Это методы, которые **выполняют действие**.

Слово `interactable` в Unity можно читать очень просто:

- `true` — кнопку можно нажимать;
- `false` — кнопку нажимать нельзя.

---

Перед ходом игра может сделать ранний выход:

```csharp
if (board[cellIndex] != 0)
{
    return;
}
```

Это означает:

- если клетка уже занята;
- дальше код выполнять не нужно.

---

```quiz
id: l2m4y2-q-early-return
kind: truefalse
text: Если клетка занята, можно сразу сделать return и не выполнять ход дальше.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (ранний выход)

- Ранний выход делает код чище.
- Сначала проверяем, можно ли ходить, и только потом выполняем сам ход.

---

```quiz
id: l2m4y2-q-switch-player
kind: single
text: Зачем нужен отдельный метод SwitchPlayer()?
answers:
  - text: Чтобы отдельно хранить и выполнять логику переключения игрока
    correct: true
  - text: Чтобы создавать новые клетки на сцене
  - text: Чтобы открывать Canvas
```

---

## Пояснение (метод-действие)

- Маленькие методы проще читать и объяснять.
- Когда у метода одна задача, урок тоже становится понятнее.

---

## Практика 2: Ограничиваем неправильные ходы

Теперь сделаем игру аккуратнее: занятую клетку нельзя нажать повторно.
После этой практики партия будет ощущаться заметно честнее и понятнее для игрока.

---

### Шаг 1: Проверки в `HandleCellClicked(int cellIndex)`

Перед вызовом `MakeMove(...)` проверьте:

- не закончилась ли партия;
- корректный ли индекс;
- пуста ли клетка.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "HandleCellClicked"
  - contains: "if (isGameOver)"
  - contains: "cellIndex < 0"
  - contains: "board[cellIndex] != 0"
  - contains: "return;"
```

---

### Шаг 2: `SetInteractable(bool isInteractable)` в `BoardCell`

Добавьте в клетку метод:

```csharp
public void SetInteractable(bool isInteractable)
{
    button.interactable = isInteractable;
}
```

```check
rules:
  - filename: "BoardCell.cs"
  - contains: "SetInteractable"
  - contains: "button.interactable"
```

---

### Шаг 3: Выключайте клетку после хода

После того как символ поставлен, удобно сделать клетку недоступной для повторного нажатия.

Это можно сделать прямо в `UpdateCell(int cellIndex)`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "boardCell.SetInteractable(false)"
```

---

### Шаг 4: Проверьте результат в игре

Проверьте, что теперь:

- X ходит первым;
- O ходит вторым;
- занятую клетку повторно нажать нельзя;
- символы в клетках соответствуют текущему игроку.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "MakeMove"
  - contains: "SwitchPlayer"
  - contains: "UpdateCell"
```

---

### 💡 Лайфхак: проверяйте логику короткими сценариями

Не нужно каждый раз играть долгую партию.
Для этого урока достаточно проверить коротко:

- X нажал;
- O нажал;
- повторный клик по X не сработал.

Так баги находятся быстрее.

---

## Итоги

На уроке мы:

- сделали очередь `X` и `O`;
- научили игру различать состояние клетки;
- запретили повторный ход в занятую клетку;
- разделили логику на небольшие понятные методы;
- превратили стартовую заготовку в узнаваемую игру на двух игроков.
