# Модуль 4, Урок 3: Крестики-нолики — List, foreach, победа и ничья

**Цель урока:** перейти на `List<BoardCell>` и `foreach`, научить игру очищать поле, а главное — добавить полную проверку победы и ничьей, чтобы после урока получилась полностью рабочая игра для двух игроков.

До этого момента игра уже реагировала на ходы и чередовала игроков.
Теперь мы доводим её до состояния, когда можно честно сыграть полную партию от начала до конца.

---

## Как превратить заготовку в полноценную игру?

Сейчас игра уже умеет:

- ставить `X` и `O`;
- чередовать ходы;
- запрещать повторный клик по занятой клетке.

Но партии пока не хватает важного финала:

- кто победил;
- когда ничья;
- как заблокировать поле после конца игры.

---

## Как работать с полем, если клеток уже много?

Когда клеток 9, неудобно обращаться к ним вручную по одной.
Игре нужен способ удобно управлять всем полем сразу:

- подготавливать клетки;
- очищать их;
- включать и выключать весь набор целиком.

---

## Теория 1: Зачем нужен `List<BoardCell>`, если массив уже есть

Когда игра становится чуть сложнее, важно не только хранить данные, но и удобно управлять объектами на сцене.
За **логику** по‑прежнему отвечает массив `board`, а вот для реальных клеток на сцене мы переходим с массива на `List<BoardCell>`.

Массив `board` отлично хранит **состояние игры**:

- где пусто;
- где `X`;
- где `O`.

Но сами кнопки и тексты на сцене удобнее хранить отдельно — в коллекции `boardCells`.

```csharp
public List<BoardCell> boardCells = new List<BoardCell>();
```

Получается полезное разделение:

- `board` — данные игры (цифры состояния);
- `boardCells` — объекты сцены (ссылки на клетки).

---

Почему здесь удобно использовать именно `List<BoardCell>`, а не массив?

- в списке проще **добавлять и удалять** элементы (`Add`, `Remove`), если поле когда‑нибудь изменится;
- у списка есть свойство `Count`, с которым удобно проходить по всем клеткам;
- `List<…>` — очень часто используемая коллекция в C#, и к ней полезно привыкнуть на реальном примере.

Важно: для фиксированного поля `3x3` можно было бы оставить и массив, но `List<BoardCell>` даёт более гибкий и привычный для Unity способ хранить ссылки на объекты сцены.

---

```quiz
id: l2m4y3-q-why-list
kind: single
text: Зачем игре нужен List<BoardCell>, если массив board уже существует?
answers:
  - text: Чтобы удобно хранить и обрабатывать визуальные клетки сцены и при необходимости менять их количество
    correct: true
  - text: Чтобы заменить Canvas
  - text: Чтобы автоматически находить победителя
```

---

## Пояснение (list и массив)

- Массив и список решают разные задачи.
- Один хранит числа, другой — ссылки на объекты.

Если коротко, **чем массив отличается от `List`**:

- **Размер**: у массива (`int[]`) размер фиксирован (`Length` задаётся один раз), у списка (`List<int>`) размер можно менять (`Add`, `Remove`, `Count` растёт или уменьшается).
- **Сценарии**: массив часто используют для **плотных данных фиксированного размера** (как наше поле `3x3`), список — когда элементов может стать больше/меньше или когда удобно дописывать в конец.
- **Синтаксис**: доступ по индексу похож (`board[i]` и `boardCells[i]`), но свойства разные: `array.Length` и `list.Count`.
- В нашем примере: состояния поля (`0/1/2`) хранятся в массиве `board`, а сами клетки сцены удобнее держать в `List<BoardCell> boardCells`.

---

### 💡 Лайфхак: когда выбирать массив, а когда List

- Если вы **на 100% уверены**, что количество элементов не изменится (например, поле `3x3`), можно спокойно использовать массив.
- Если размер коллекции может меняться или вы не уверены — берите `List`: добавлять/удалять в нём проще.
- В большинстве учебных и игровых задач можно всегда использовать `List` — по производительности он близок к массиву, потому что внутри сам использует массив.

---

```quiz
id: l2m4y3-q-list-vs-array
kind: multiple
text: В чём разница между массивом и List в C#? (выберите 2)
answers:
  - text: У массива фиксированный размер, а у List размер можно менять (Add/Remove)
    correct: true
  - text: У массива свойство Length, у List — Count
    correct: true
  - text: Массив всегда работает быстрее, чем List
  - text: List нельзя хранить ссылки на объекты
```

---

## Пояснение (что где хранить)

- Здесь важно разделить **два разных набора данных**:
  - массив `board` с числами (`0/1/2`) — это чисто **игровая логика** (кто где стоит);
  - коллекция `boardCells` со ссылками на `BoardCell` — это **объекты сцены** (кнопки и текст).
- То, что мы используем именно `List<BoardCell>`, связано не с логикой, а с удобством работы с набором объектов: размер можно менять, есть `Count` и знакомый для Unity паттерн «список ссылок».

---

## Практика 1: Собираем поле в список

Сейчас сделаем так, чтобы игра централизованно знала все клетки на сцене.
После этой практики с полем станет проще работать целиком, а не по одной кнопке за раз.

---

### Шаг 1: Замените массив клеток на список в `TicTacToeGame.cs`

Откройте `TicTacToeGame.cs`. Найдите строку:

```csharp
public BoardCell[] boardCells;
```

Замените её на:

```csharp
public List<BoardCell> boardCells = new List<BoardCell>();
```

И **в самом начале файла**, перед `public class TicTacToeGame`, добавьте:

```csharp
using System.Collections.Generic;
```

> **Зачем нужен `using System.Collections.Generic;`?** Эта строка подключает пространство имён, где живёт тип `List`. Без неё C# просто не поймёт, что такое `List<BoardCell>`, и выдаст ошибку компиляции.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "using System.Collections.Generic;"
  - contains: "List<BoardCell>"
```

---

### Шаг 2: Обновите `PrepareBoardCells()`

После замены массива на список метод `PrepareBoardCells` нужно немного обновить — заменить `boardCells.Length` на `boardCells.Count`:

```csharp
public void PrepareBoardCells()
{
    for (int i = 0; i < boardCells.Count; i++)
    {
        boardCells[i].Setup(this, i);
    }
}
```

> `boardCells.Count` означает «сколько клеток сейчас лежит в списке». Это то же самое, что `Length` у массива — только для `List`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "PrepareBoardCells"
  - contains: "boardCells.Count"
  - contains: "Setup(this, i)"
```

---

### Шаг 3: Подключите клетки в инспекторе

После смены типа с массива на список поле в Inspector изменит вид — теперь это раскрывающийся список со значком `+`.

1. Выделите `GameManager` в Hierarchy.
2. В Inspector найдите компонент `TicTacToeGame`. Увидите поле **`Board Cells`** — теперь это список.
3. Нажмите кнопку `+` восемь раз — появится 9 слотов.
4. Перетащите клетки из Hierarchy в слоты строго по порядку:
   - `Cell0` в первый слот,
   - `Cell1` во второй,
   - ...
   - `Cell8` в последний.
5. Нажмите **Play** и убедитесь, что клетки работают как раньше.

```check
rules:
  - component_exists:
      object: "GameManager"
      type: "TicTacToeGame"
```

---

### 💡 Лайфхак: данные и объекты сцены лучше хранить отдельно

Если числа состояния лежат отдельно от кнопок и текста, игру потом проще:

- отлаживать;
- расширять;
- объяснять другому человеку.

---

## Перерыв

---

Теперь игра умеет удобно обращаться ко всем клеткам.
Значит, можно не только подготавливать поле, но и полностью управлять его состоянием.

---

## Как не писать 9 одинаковых строк?

Если нужно очистить всё поле, неудобно писать:

- `cell0.ClearCell();`
- `cell1.ClearCell();`
- `cell2.ClearCell();`
- и ещё 6 похожих строк.

Нужен способ сказать: «сделай одно и то же для всех клеток».

---

## Теория 2: `foreach`, очистка поля и подготовка новой партии

Когда клеток много, самый удобный следующий шаг — научиться применять одно действие ко всему полю сразу.
Именно поэтому `foreach` здесь появляется не как абстрактная тема, а как реальный инструмент для игры.

Когда у нас есть список, очень удобно использовать `foreach`:

```csharp
foreach (BoardCell boardCell in boardCells)
{
    boardCell.ClearCell();
}
```

Через `foreach` удобно:

- очищать всё поле;
- включать и выключать клетки;
- применять одно действие ко всем объектам.

> **Пример на выбор: `foreach` или `for`**
>
> - Если вам нужны **только элементы** (без индекса) — чаще всего удобнее `foreach`:
>
> ```csharp
> foreach (BoardCell boardCell in boardCells)
> {
>     boardCell.ClearCell();
> }
> ```
>
> - Если вам нужен **и индекс, и элемент** (например, чтобы использовать `i` для массива `board`) — удобнее `for`:
>
> ```csharp
> for (int i = 0; i < boardCells.Count; i++)
> {
>     BoardCell boardCell = boardCells[i];
>     // i — индекс клетки, его можно использовать для board[i]
>     boardCell.ClearCell();
> }
> ```

---

Для этого полезно сделать отдельные методы:

- `ClearCell()` — очищает одну клетку;
- `ResetBoard()` — очищает массив и визуал;
- `SetBoardInteractable(bool isInteractable)` — включает или выключает всё поле.

У каждого метода здесь одна понятная задача.

---

```quiz
id: l2m4y3-q-foreach-why
kind: single
text: Почему foreach удобен в этом уроке?
answers:
  - text: Он помогает обработать все клетки без копирования одинакового кода
    correct: true
  - text: Он заменяет массив board
  - text: Он нужен только для звука
```

---

## Пояснение (foreach)

- `foreach` хорошо подходит для набора похожих объектов.
- В этом уроке это все клетки поля.

---

```quiz
id: l2m4y3-q-reset-board
kind: truefalse
text: ResetBoard() удобно делать через цикл, потому что нужно привести в порядок всё поле, а не одну клетку.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (очистка поля)

- После партии игре нужно быстро вернуться в чистое состояние.
- Отдельные методы делают это гораздо удобнее.

---

## Практика 2: Очищаем поле и управляем клетками

Теперь добавим методы, которые помогают игре быстро подготовить новую партию.
После этой практики поле можно будет полностью очистить и снова сделать готовым к игре.

---

### Шаг 1: Добавьте `ClearCell()` в `BoardCell`

Откройте `BoardCell.cs`. Добавьте метод в класс:

```csharp
public void ClearCell()
{
    symbolText.text = string.Empty;
    SetInteractable(true);
}
```

> **Как работает:**
>
> 1. `symbolText.text = string.Empty` — убирает символ из текстового поля. `string.Empty` — это пустая строка `""`, но более явная запись.
> 2. `SetInteractable(true)` — включает кнопку обратно, чтобы по ней снова можно было нажать.

```check
rules:
  - filename: "BoardCell.cs"
  - contains: "ClearCell"
  - contains: "symbolText.text = string.Empty"
```

---

### Шаг 2: Добавьте `ResetBoard()` в `TicTacToeGame`

Этот метод сбрасывает все данные поля и визуал клеток.
Добавьте в класс `TicTacToeGame`:

```csharp
public void ResetBoard()
{
    for (int i = 0; i < board.Length; i++)
    {
        board[i] = 0;
    }

    foreach (BoardCell boardCell in boardCells)
    {
        boardCell.ClearCell();
    }

    currentPlayer = 1;
    isGameOver = false;
}
```

> **Как работает по шагам:**
>
> 1. Цикл `for` проходит по массиву `board` и ставит `0` в каждую клетку — поле снова пустое.
> 2. `foreach` проходит по всем визуальным клеткам и вызывает `ClearCell()` — убирает символы и включает кнопки.
> 3. `currentPlayer = 1` — X снова ходит первым.
> 4. `isGameOver = false` — партия не закончена, можно играть.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "ResetBoard"
  - contains: "board[i] = 0"
  - contains: "foreach (BoardCell boardCell in boardCells)"
  - contains: "currentPlayer = 1"
```

---

### Шаг 3: Добавьте `SetBoardInteractable(bool isInteractable)`

Этот метод нужен для быстрой блокировки всего поля — например, после победы.
Добавьте в `TicTacToeGame`:

```csharp
public void SetBoardInteractable(bool isInteractable)
{
    foreach (BoardCell boardCell in boardCells)
    {
        boardCell.SetInteractable(isInteractable);
    }
}
```

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "SetBoardInteractable"
  - contains: "boardCell.SetInteractable"
```

---

### 💡 Лайфхак: `foreach` лучше всего объясняется на реальных объектах

Когда один цикл очищает всё поле, польза `foreach` становится очевидной намного быстрее, чем на абстрактных примерах.

---

## Как игре самой ответить на вопрос «кто победил?»

После каждого хода игра должна быстро проверить:

- есть ли линия из трёх одинаковых символов;
- осталось ли место на поле;
- нужно ли завершить партию.

---

## Теория 3: Методы, которые возвращают ответ

К этому моменту игра уже умеет выполнять действия.
Теперь ей нужно научиться отвечать на важные игровые вопросы: есть ли победа, остались ли пустые клетки и пора ли завершать партию.

Есть методы, которые делают действие:

- `MakeMove(...)`
- `SwitchPlayer()`
- `ResetBoard()`

А есть методы, которые **возвращают результат проверки**.
Это методы с типом `bool`.

Например, вместо того чтобы каждый раз писать:

```csharp
if (cellIndex < 0 || cellIndex >= board.Length)
{
    return;
}
```

можно вынести проверку в отдельный метод:

```csharp
public bool IsCellIndexValid(int cellIndex)
{
    return cellIndex >= 0 && cellIndex < board.Length;
}
```

И использовать его в коде:

```csharp
if (!IsCellIndexValid(cellIndex))
{
    return;
}
```

Такой метод читается как вопрос, а `return` внутри него возвращает игре готовый ответ.

В этом уроке будут `bool`-методы:

- `bool MatchesLine(...)`
- `bool CheckWinner(...)`
- `bool IsBoardFull()`

---

Такой метод удобно читать как вопрос:

```csharp
if (CheckWinner(currentPlayer))
{
    FinishGameWithWinner(currentPlayer);
}
```

Здесь `CheckWinner(currentPlayer)` возвращает:

- `true`, если игрок победил;
- `false`, если нет.

---

В крестиках-ноликах удобно разбить проверку на маленькие части:

- `MatchesLine(...)` проверяет одну линию;
- `CheckWinner(...)` собирает все победные линии;
- `IsBoardFull()` проверяет, остались ли пустые клетки.

Если победителя нет, а пустых клеток уже нет, это ничья.

---

```quiz
id: l2m4y3-q-bool-method
kind: truefalse
text: Метод с типом bool возвращает true или false.
answers:
  - text: True
    correct: true
  - text: False
```

---

## Пояснение (bool-метод)

- `bool`-метод не рисует сам по себе UI.
- Он возвращает игре ответ, по которому можно принять решение (`IsCellIndexValid`, `CheckWinner`, `IsBoardFull` и т.д.).

---

```quiz
id: l2m4y3-q-draw
kind: multiple
text: Когда в игре ничья? (выберите 2)
answers:
  - text: Поле заполнено
    correct: true
  - text: Победителя нет
    correct: true
  - text: Игрок нажал Space
  - text: Игра не запускалась
```

---

## Пояснение (ничья)

- Ничья наступает не просто от заполненного поля.
- Важно, чтобы при этом ни у кого не было победной линии.

---

## Практика 3: Делаем полную проверку победы и ничьей

Сейчас именно тот момент, когда игра станет полностью рабочей для двух игроков.
После этой практики можно будет провести полную партию с победой или ничьей.

---

### Шаг 1: Добавьте `IsCellIndexValid(int cellIndex)` в `TicTacToeGame`

Добавьте метод:

```csharp
public bool IsCellIndexValid(int cellIndex)
{
    return cellIndex >= 0 && cellIndex < board.Length;
}
```

Теперь обновите `HandleCellClicked` — замените ручную проверку индекса на вызов нового метода:

```csharp
if (!IsCellIndexValid(cellIndex))
{
    return;
}
```

> `!` перед вызовом означает «если НЕ корректный». Это удобнее, чем писать `cellIndex < 0 || cellIndex >= board.Length` каждый раз.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "bool IsCellIndexValid"
  - contains: "return cellIndex >= 0 && cellIndex < board.Length"
  - contains: "if (!IsCellIndexValid(cellIndex))"
```

---

### Шаг 2: Добавьте `MatchesLine(int player, int a, int b, int c)`

Этот метод проверяет одну линию — три клетки по индексам `a`, `b`, `c` — и возвращает `true`, если все три принадлежат одному игроку.

Добавьте в `TicTacToeGame`:

```csharp
public bool MatchesLine(int player, int a, int b, int c)
{
    return board[a] == player && board[b] == player && board[c] == player;
}
```

> **Как читать эту строку:** «все три клетки содержат номер этого игрока». Если хотя бы одна отличается — возвращает `false`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "MatchesLine"
  - contains: "return board[a] == player && board[b] == player && board[c] == player"
```

---

### Шаг 3: Добавьте `CheckWinner(int player)`

Этот метод проверяет все возможные победные линии: 3 строки, 3 столбца и 2 диагонали.

Добавьте в `TicTacToeGame`:

```csharp
public bool CheckWinner(int player)
{
    // строки
    if (MatchesLine(player, 0, 1, 2)) return true;
    if (MatchesLine(player, 3, 4, 5)) return true;
    if (MatchesLine(player, 6, 7, 8)) return true;

    // столбцы
    if (MatchesLine(player, 0, 3, 6)) return true;
    if (MatchesLine(player, 1, 4, 7)) return true;
    if (MatchesLine(player, 2, 5, 8)) return true;

    // диагонали
    if (MatchesLine(player, 0, 4, 8)) return true;
    if (MatchesLine(player, 2, 4, 6)) return true;

    return false;
}
```

> **Как устроено поле:**
>
> ```
> 0 | 1 | 2
> ---------
> 3 | 4 | 5
> ---------
> 6 | 7 | 8
> ```
>
> Индексы идут слева направо, сверху вниз — именно так мы именовали клетки в первом уроке.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "CheckWinner"
  - contains: "MatchesLine(player, 0, 1, 2)"
  - contains: "MatchesLine(player, 0, 4, 8)"
```

---

### Шаг 4: Добавьте `IsBoardFull()`

Этот метод проходит по массиву и проверяет, осталась ли хотя бы одна пустая клетка.

```csharp
public bool IsBoardFull()
{
    for (int i = 0; i < board.Length; i++)
    {
        if (board[i] == 0)
        {
            return false;
        }
    }

    return true;
}
```

> **Как работает:** если нашли хотя бы одну клетку с `0` — сразу возвращаем `false` (поле ещё не заполнено). Если прошли все 9 клеток и `0` не нашли — возвращаем `true`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "IsBoardFull"
  - contains: "board[i] == 0"
```

---

### Шаг 5: Добавьте `FinishGameWithWinner(int winner)`

Когда победитель найден:

```csharp
public void FinishGameWithWinner(int winner)
{
    isGameOver = true;
    SetBoardInteractable(false);
    Debug.Log("Победил игрок: " + winner);
}
```

> `Debug.Log(...)` в Unity выводит сообщение в окно **Console**. Это удобный способ быстро показать результат игры, даже если отдельного текста на экране ещё нет. Консоль открывается через меню `Window → General → Console`.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "FinishGameWithWinner"
  - contains: "isGameOver = true"
  - contains: "SetBoardInteractable(false)"
  - contains: "Debug.Log"
```

---

### Шаг 6: Добавьте `FinishGameWithDraw()`

Теперь отдельно метод для ничьей — логика почти такая же:

```csharp
public void FinishGameWithDraw()
{
    isGameOver = true;
    SetBoardInteractable(false);
    Debug.Log("Ничья!");
}
```

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "FinishGameWithDraw"
```

---

### Шаг 7: Соберите проверки в `MakeMove(int cellIndex)`

Найдите метод `MakeMove` и обновите его — после записи хода добавьте проверку победы и ничьей:

```csharp
public void MakeMove(int cellIndex)
{
    board[cellIndex] = currentPlayer;
    UpdateCell(cellIndex);

    if (CheckWinner(currentPlayer))
    {
        FinishGameWithWinner(currentPlayer);
        return;
    }

    if (IsBoardFull())
    {
        FinishGameWithDraw();
        return;
    }

    SwitchPlayer();
}
```

> **Порядок важен:** сначала проверяем победу, потом ничью и только потом переключаем игрока. Если партия уже закончилась — выходим через `return`, не переключая очередь.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "if (CheckWinner(currentPlayer))"
  - contains: "FinishGameWithWinner(currentPlayer)"
  - contains: "if (IsBoardFull())"
  - contains: "FinishGameWithDraw()"
```

---

### Шаг 8: Проверьте в Console результат игры

1. Нажмите **Play** в Unity.
2. Откройте окно Console: `Window → General → Console`.
3. Сыграйте до победы — в Console должно появиться `Победил игрок: 1` (или `2`).
4. Проверьте ничью: заполните всё поле без победной линии — должно появиться `Ничья!`.
5. После победы убедитесь, что клетки больше не реагируют на клики.

```check
rules:
  - filename: "TicTacToeGame.cs"
  - contains: "if (isGameOver)"
```

---

### 💡 Лайфхак: тестируйте не «как получится», а по сценариям

Быстрее всего проверять игру через короткие сценарии:

- победа по строке;
- победа по столбцу;
- победа по диагонали;
- ничья.

Так ошибки в логике находятся заметно быстрее.

---

## Итоги

На уроке мы:

- перешли на `List<BoardCell>`;
- научились использовать `foreach` на реальном поле;
- сделали методы для очистки и подготовки новой партии;
- познакомились с `bool`-методами;
- добавили победу и ничью;
- довели проект до полностью рабочей игры для двух игроков.
