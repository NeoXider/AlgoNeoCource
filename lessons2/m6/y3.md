# Модуль 6, Урок 3: GameManager и очки

**Цель урока:** ввести общий менеджер игры (GameManager), счёт очков, состояние «игра окончена» и связь с UI через события. Добавить постепенное ускорение темпа.

---

Игрок, враги и астероиды делают своё дело. Но кто считает очки, решает, что игра закончилась, и обновляет надпись на экране? Для этого нужен один общий объект — **GameManager**.

---

## Теория 1: Роль GameManager

**GameManager** — скрипт на одном объекте сцены, который хранит общее состояние игры:

- счёт (очки);
- флаг «игра окончена» (`isGameOver`);
- при необходимости — множитель сложности (скорость врагов/астероидов).

Важно не менять эти данные отовсюду напрямую. Лучше:

- увеличивать счёт через метод `AddScore(int points)`;
- завершать игру через метод `FinishGame()`.

Так все изменения проходят через один «контролёр», код остаётся предсказуемым и аккуратным.

Иногда такие данные удобно прятать за **свойствами C#**. Свойство — это как «умная переменная», где вы сами решаете, что произойдёт при получении или изменении значения. Такое поле называется **свойство**.

```csharp
public int Score { get; private set; }
public bool IsGameOver { get; private set; }
```

- `get` — как «взять значение» (читать можно в любом скрипте);
- `private set` — как «установить значение», но только внутри самого GameManager.

Так другие скрипты смогут читать `Score` и `IsGameOver`, но менять их смогут только методы самого менеджера (`AddScore`, `FinishGame`).

> **Пояснение:**  
> - Свойство работает примерно как обычная переменная, но через него можно добавить свою логику при чтении или записи значения: например, автоматически вызывать дополнительные методы или проверки.
> - Это инструмент для более «контролируемого» доступа к данным.
> - Учтите, что такие свойства **не отображаются в инспекторе Unity**! Для вывода значения в редакторе используйте обычные поля с атрибутом `[SerializeField]` или `public` переменные.

---

```quiz
id: l2m6y3-q-gamemanager-role
kind: single
text: Зачем нужен GameManager?
answers:
  - text: Хранить общее состояние игры (очки, конец игры) и менять его через методы
    correct: true
  - text: Управлять движением корабля
  - text: Создавать астероиды
```

---

## Пояснение

- GameManager не подменяет игрока или спавнер — он только хранит счёт и факт окончания игры и даёт другим скриптам вызывать AddScore и FinishGame.

---

```quiz
id: l2m6y3-q-addscore
kind: single
text: Почему счёт лучше увеличивать через AddScore(points), а не напрямую?
answers:
  - text: Чтобы все изменения шли через один объект, проще контролировать и подписывать UI
    correct: true
  - text: Чтобы счёт не сохранялся
  - text: Чтобы игра работала быстрее
```

---

## Пояснение

- Один метод AddScore позволяет в одном месте проверять isGameOver, обновлять UI по событию и не искать по проекту, где ещё меняется score.

---

## Теория 2: События и UI

Чтобы UI (текст очков, панель Game Over) обновлялся при изменении счёта или при проигрыше, не обязательно давать UI прямую ссылку на переменную счёта. Удобнее использовать **события**:

- При вызове `AddScore` GameManager вызывает событие «счёт изменился» и передаёт новое значение.
- UI подписывается на это событие и обновляет надпись.
- При вызове `FinishGame` GameManager вызывает событие «игра окончена»; UI подписывается и показывает панель Game Over.

В Unity для этого используют `UnityEvent`. Если нужно передать число (новый счёт), делают класс-обёртку: `UnityEvent<int>`. В коде это выглядит так:

```csharp
[Serializable]
public class ScoreChangedEvent : UnityEvent<int> { }

[SerializeField] private ScoreChangedEvent onScoreChanged = new ScoreChangedEvent();

public void AddScore(int points)
{
    if (!isGameOver)
    {
        score += points;
        onScoreChanged.Invoke(score);
    }
}
```

> **Как работает связка события и UI по шагам:**
>
> 1. `AddScore(points)` увеличивает внутренний счёт.
> 2. Сразу после этого вызывается `onScoreChanged.Invoke(score)` — GameManager сообщает: «счёт изменился, вот новое значение».
> 3. На событие заранее подписан UI (например, `GameHud`), у него есть метод `UpdateScoreText(int value)`.
> 4. Когда событие срабатывает, этот метод вызывается с новым числом, и текст на экране обновляется.

UI в `Start()` подписывается: `gameManager.OnScoreChanged.AddListener(UpdateScoreText);`, а метод `UpdateScoreText(int value)` выводит значение в текст.

---

```quiz
id: l2m6y3-q-unityevent
kind: single
text: Зачем подписывать UI на событие счёта, а не читать score каждый кадр?
answers:
  - text: Обновлять только когда счёт реально изменился, без лишних связей
    correct: true
  - text: Чтобы счёт не отображался
  - text: Чтобы событие работало быстрее
```

---

## Пояснение

- Подписка на событие — аккуратный способ связи: GameManager не знает про конкретный UI, а UI просто реагирует на «счёт изменился».

---

```quiz
id: l2m6y3-q-unityevent-int
kind: single
text: Что означает int в UnityEvent<int>?
answers:
  - text: Через событие можно передать целое число, например новый счёт
    correct: true
  - text: Событие сработает 1 раз
  - text: Событие работает только с int-переменными внутри одного класса
```

---

## Пояснение

- `UnityEvent<int>` удобно использовать, когда вместе с событием нужно отправить число, например обновлённый счёт.

---

## Практика 1: GameManager и счёт

**Задача:**

1. Создайте пустой объект `GameController` (или `GameManager`), добавьте скрипт `GameManager.cs`.
2. Добавьте поля или свойства для счёта и флага конца игры. Удобно сразу сделать их **свойствами только для чтения снаружи**:

```csharp
public int Score { get; private set; }
public bool IsGameOver { get; private set; }
```

Счёт лучше увеличивать только в методе `AddScore(int points)` и только если `!IsGameOver`. Внутри GameManager можно менять свойства через `Score += points;` и `IsGameOver = true;`.
3. При уничтожении астероида или врага вызовите `AddScore(10)` у `GameManager`. Проще всего передать ссылку на `GameManager` через инспектор или получить её заранее в `Start()`.
4. Выведите счёт на UI: можно сразу подписать `GameHud` на событие изменения счёта или временно обновлять текст внутри `AddScore`.

**Упрощённый пример GameManager (без событий, только счёт и конец игры):**

```csharp
[SerializeField] private TMP_Text scoreText; // ссылка из инспектора

public int Score { get; private set; }
public bool IsGameOver { get; private set; }
[SerializeField] private TMP_Text scoreText; // ссылка из инспектора

public void AddScore(int points)
{
    if (!IsGameOver)
    {
        Score += points;
        scoreText.text = "Очки: " + Score;
    }
}

public void FinishGame()
{
    if (!IsGameOver)
    {
        IsGameOver = true;
        // показать панель Game Over
    }
}
```

```check
rules:
  - filename: "GameManager.cs"
  - contains: "int Score { get; private set; }"
  - contains: "bool IsGameOver { get; private set; }"
  - contains: "AddScore"
  - contains: "FinishGame"
```

---

## Практика 2: Game Over и остановка игры

**Задача:**

1. В GameManager в `FinishGame()` устанавливайте `isGameOver = true` и показывайте панель Game Over (например, `gameOverPanel.SetActive(true)`).
2. Когда корабль игрока получает урон или сталкивается с врагом/астероидом, вызывайте `GameManager.FinishGame()`.
3. В скрипте корабля игрока и в спавнере в начале `Update()` проверяйте: если `gameManager.isGameOver` — не выполнять движение и не спавнить (обернуть логику в `if (!gameManager.isGameOver) { ... }`).

```check
rules:
  - filename: "GameManager.cs"
  - contains: "FinishGame"
  - contains: "gameOverPanel"
```

---

## Перерыв

---

Игра уже умеет считать очки и завершаться. Дальше — сделать её постепенно сложнее без новых механик.

---

## Теория 3: Рост сложности

Сложность можно поднимать не новыми врагами, а изменением уже существующих параметров:

- увеличивать скорость астероидов и врагов со временем;
- или уменьшать интервал спавна.

Для этого в GameManager хранят, например, `speedMultiplier`. Раз в N секунд его увеличивают (`InvokeRepeating(nameof(IncreaseDifficulty), 10f, 10f)`). Астероиды и враги при движении умножают свою базовую скорость на `gameManager.SpeedMultiplier`. Так темп постепенно растёт.

---

> **Как работает постепенное усложнение через InvokeRepeating:**
>
> 1. В `Start()` GameManager один раз вызывается `InvokeRepeating(nameof(IncreaseDifficulty), 10f, 10f)`.
> 2. Unity ждёт 10 секунд, затем вызывает `IncreaseDifficulty()` и повторяет это каждые 10 секунд.
> 3. Внутри `IncreaseDifficulty` вы увеличиваете `speedMultiplier` (например, `speedMultiplier += 0.2f;`).
> 4. Скрипты движения берут скорость как `baseSpeed * speedMultiplier`, поэтому с каждым вызовом метода игра становится чуть быстрее.

---

```quiz
id: l2m6y3-q-difficulty
kind: single
text: Как проще всего сделать игру постепенно сложнее без новых механик?
answers:
  - text: Увеличивать со временем скорость или частоту появления уже существующих объектов
    correct: true
  - text: Добавить нового босса
  - text: Уменьшить экран
```

---

## Пояснение

- Подкрутка скорости и интервалов — самый простой и честный способ усложнения для игрока.

---

```quiz
id: l2m6y3-q-speed-multiplier
kind: single
text: Для чего нужен speedMultiplier в GameManager?
answers:
  - text: Чтобы постепенно усиливать скорость уже существующих объектов
    correct: true
  - text: Чтобы хранить очки игрока
  - text: Чтобы перезапускать сцену
```

---

## Пояснение

- Множитель скорости позволяет менять темп игры из одного места, не переписывая каждый скрипт отдельно.

---

## Практика 3: Постепенное ускорение

**Задача:**

1. В GameManager добавьте `speedMultiplier` (float, начальное значение 1f) и метод `IncreaseDifficulty()`, который увеличивает его на небольшой шаг (например, на 0.2f).
2. В `Start()` GameManager вызовите `InvokeRepeating(nameof(IncreaseDifficulty), 10f, 10f)` — раз в 10 секунд сложность растёт.
3. В скриптах движения астероидов и врагов используйте скорость с учётом множителя (например, брать `baseSpeed * gameManager.SpeedMultiplier`). Не забудьте проверять `!isGameOver` перед вызовом IncreaseDifficulty в GameManager.

```check
rules:
  - filename: "GameManager.cs"
  - contains: "IncreaseDifficulty"
  - contains: "InvokeRepeating"
```

---

### 💡 Лайфхак

Не обязательно усложнять игру новыми системами. Часто достаточно аккуратно подкрутить уже существующие параметры: скорость, интервалы спавна, урон.

---

## Итоги

На уроке мы:

- ввели GameManager для хранения счёта и состояния игры;
- добавили AddScore и FinishGame и вызывали их при уничтожении врагов/астероидов и при проигрыше;
- подключили UI к счёту и к панели Game Over (через прямую связь или через события);
- остановили управление и спавн при isGameOver;
- добавили постепенное ускорение через speedMultiplier и InvokeRepeating(IncreaseDifficulty).
